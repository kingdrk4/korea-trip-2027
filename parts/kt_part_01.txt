<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<title>✈️ KOREA TRIP 2027 💗</title>
<style>
:root{
  --bg:#fff7fb;
  --bg2:#fff0f7;
  --card:#ffffff;
  --pink:#ff5c9b;
  --pink2:#ff8fbd;
  --pink3:#ffd6e7;
  --deep:#7f2850;
  --text:#3f2633;
  --muted:#8b6878;
  --line:#f3c7da;
  --green:#49b88c;
  --gold:#d79a3c;
  --shadow:0 10px 30px rgba(194,73,124,.10);
}
*{box-sizing:border-box}
html{scroll-behavior:smooth}
body{
  margin:0;
  background:
    radial-gradient(circle at 10% 0%,#ffe5f0 0,transparent 28%),
    linear-gradient(180deg,var(--bg),#fff 45%,var(--bg2));
  color:var(--text);
  font-family:-apple-system,BlinkMacSystemFont,"Segoe UI","PingFang SC","Microsoft YaHei",Arial,sans-serif;
}
header{
  position:sticky;top:0;z-index:50;
  background:rgba(255,247,251,.88);
  backdrop-filter:blur(18px);
  border-bottom:1px solid rgba(243,199,218,.8);
  padding:12px 14px 10px;
}
.brand{
  display:flex;align-items:center;justify-content:space-between;gap:10px;
  max-width:1100px;margin:auto;
}
.brand h1{font-size:20px;margin:0;color:var(--deep);letter-spacing:.3px}
.brand .sub{font-size:11px;color:var(--muted);margin-top:2px}
nav{
  max-width:1100px;margin:10px auto 0;
  display:flex;gap:8px;overflow:auto;scrollbar-width:none;
}
nav::-webkit-scrollbar{display:none}
.tab{
  flex:0 0 auto;border:1px solid var(--line);background:#fff;color:var(--muted);
  border-radius:999px;padding:8px 12px;font-weight:700;font-size:12px;
}
.tab.active{background:var(--pink);border-color:var(--pink);color:white}
main{max-width:1100px;margin:auto;padding:14px 14px 100px}
.view{display:none}.view.active{display:block}

.countdownHero{
  position:relative;overflow:hidden;
  border:1px solid #ffc3dc;
  background:linear-gradient(135deg,#fff,#ffe8f2 55%,#ffd6e7);
  border-radius:24px;padding:20px 18px;box-shadow:var(--shadow);
  margin-bottom:14px;
}
.countdownHero:after{
  content:"♡";position:absolute;right:18px;top:2px;font-size:92px;
  color:rgba(255,92,155,.12);font-weight:900;
}
.countLabel{font-size:12px;color:var(--deep);font-weight:800}
.countBig{font-size:38px;font-weight:900;color:var(--pink);letter-spacing:-1px;margin:4px 0}
.countMeta{font-size:12px;color:var(--muted)}
.flightStrip{
  margin-top:14px;display:grid;grid-template-columns:1fr auto 1fr;gap:10px;align-items:center;
  background:rgba(255,255,255,.72);border:1px solid #f7d7e4;border-radius:16px;padding:12px;
}
.flightCity{font-weight:900;font-size:18px;color:var(--deep)}
.flightTime{font-size:12px;color:var(--muted);margin-top:4px}.plane{font-size:22px;color:var(--pink)}

.grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}
@media(min-width:760px){
  .grid3{grid-template-columns:repeat(3,minmax(0,1fr))}
  .grid4{grid-template-columns:repeat(4,minmax(0,1fr))}
  .twoCol{display:grid;grid-template-columns:1.05fr .95fr;gap:12px}
}
.card{
  background:var(--card);border:1px solid var(--line);border-radius:18px;padding:14px;
  box-shadow:0 6px 18px rgba(194,73,124,.06);margin-bottom:10px;
}
.card h3{font-size:15px;margin:0 0 7px;color:var(--deep)}
.kpi{font-size:23px;font-weight:900;color:var(--pink)}
.small{font-size:12px;color:var(--muted);line-height:1.55}
.sectionTitle{font-size:18px;margin:18px 2px 10px;color:var(--deep)}
.badge{
  display:inline-block;padding:4px 8px;border-radius:999px;background:#fff0f6;
  color:#b53d71;font-size:11px;font-weight:700;margin:2px 4px 2px 0;border:1px solid #ffd0e2;
}
.badge.good{background:#eefbf5;color:#2c8f68;border-color:#bfead8}
.badge.gold{background:#fff8e9;color:#a36a12;border-color:#f6dfb2}
.row{display:flex;gap:10px;align-items:flex-start}.grow{flex:1}
.check{
  width:22px;height:22px;border-radius:7px;border:1px solid #e8a8c4;background:#fff;
  color:white;display:flex;align-items:center;justify-content:center;flex:0 0 auto;cursor:pointer;
}
.check.done{background:var(--pink);border-color:var(--pink)}
.doneText{text-decoration:line-through;opacity:.55}
.day{
  position:relative;border-left:4px solid var(--pink2);padding-left:14px;
}
.day:before{
  content:"";position:absolute;left:-8px;top:18px;width:12px;height:12px;border-radius:50%;
  background:var(--pink);border:3px solid #fff4f8;
}
.time{font-size:11px;color:var(--pink);font-weight:900}
.place{font-weight:900;font-size:16px;color:var(--deep);margin:3px 0}
.meta{font-size:12px;color:var(--muted);line-height:1.55}
.rec{
  margin-top:9px;background:#fff4f8;border:1px solid #ffd9e8;border-radius:12px;padding:9px;
  font-size:12px;line-height:1.55;color:#6f4054;
}
.shot{
  font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:11px;
  background:#fff7fb;padding:8px;border-radius:9px;margin-top:8px;color:#7c5365;border:1px dashed #efbfd3;
}
.progress{height:9px;background:#f9dbe7;border-radius:999px;overflow:hidden}
.progress>span{display:block;height:100%;background:linear-gradient(90deg,var(--pink),#ff9bc2);width:0}
.priceRow{display:flex;justify-content:space-between;gap:12px;font-size:12px;margin:6px 0}
.priceRow b{color:var(--deep)}
.mapBtn{
  display:inline-block;margin-top:8px;border:1px solid #f0b9d0;background:#fff5f9;color:#b3396b;
  border-radius:10px;padding:7px 9px;font-size:11px;text-decoration:none;font-weight:800;
}
input,textarea,select{
  width:100%;border:1px solid var(--line);background:#fffafd;color:var(--text);
  border-radius:12px;padding:10px;font:inherit;
}
textarea{min-height:120px}
.saveBtn{
  bac